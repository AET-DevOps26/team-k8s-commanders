openapi: 3.0.3
info:
  title: CareDesk API
  version: 1.0.0
  description: |
    REST API for the CareDesk platform.

    All resources are served under the `/api/v1` base path. Error responses follow
    RFC 9457 (Problem Details for HTTP APIs) and are returned with the
    `application/problem+json` media type.
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
servers:
  - url: /api/v1
    description: Versioned base path
security:
  - bearerAuth: []
tags:
  - name: Auth
    description: Authentication and profile management
  - name: Users
    description: User account and role administration
  - name: Clinics
    description: Clinic overview and staff management
  - name: Patients
    description: Patient profile and history operations
  - name: Doctors
    description: Doctor schedule and clinical workflow operations
  - name: Appointments
    description: Appointment lifecycle operations
  - name: Notifications
    description: Reminder and delivery operations
  - name: AI Assistant
    description: GenAI-powered patient history and clinical guidance queries
paths:
  /auth/register:
    post:
      tags: [Auth]
      summary: Register a new patient account
      operationId: registerUser
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: Registered successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSession'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /auth/login:
    post:
      tags: [Auth]
      summary: Authenticate a user
      operationId: loginUser
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthSession'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /auth/logout:
    post:
      tags: [Auth]
      summary: Log out the current session
      operationId: logoutUser
      responses:
        '204':
          description: Logged out
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /users:
    get:
      tags: [Users]
      summary: List users (paginated)
      operationId: listUsers
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/SizeParam'
      responses:
        '200':
          description: Paginated user list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUserProfileResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /users/{userId}:
    get:
      tags: [Users]
      summary: Get a user by id
      operationId: getUserById
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: User details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      tags: [Users]
      summary: Replace a user profile
      operationId: replaceUser
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserProfile'
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /users/{userId}/password:
    put:
      tags: [Users]
      summary: Change a user's password
      operationId: changeUserPassword
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordChangeRequest'
      responses:
        '204':
          description: Password changed
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /clinics/{clinicId}:
    get:
      tags: [Clinics]
      summary: Get clinic details
      operationId: getClinicById
      parameters:
        - $ref: '#/components/parameters/ClinicId'
      responses:
        '200':
          description: Clinic overview
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Clinic'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /clinics/{clinicId}/staff:
    get:
      tags: [Clinics]
      summary: List clinic staff (paginated)
      operationId: listClinicStaff
      parameters:
        - $ref: '#/components/parameters/ClinicId'
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/SizeParam'
      responses:
        '200':
          description: Paginated staff list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUserProfileResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /patients/{patientId}:
    get:
      tags: [Patients]
      summary: Get patient profile
      operationId: getPatientById
      parameters:
        - $ref: '#/components/parameters/PatientId'
      responses:
        '200':
          description: Patient profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /patients/{patientId}/appointments:
    get:
      tags: [Patients]
      summary: List a patient's appointments (paginated)
      operationId: listPatientAppointments
      parameters:
        - $ref: '#/components/parameters/PatientId'
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/SizeParam'
      responses:
        '200':
          description: Paginated patient appointments
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAppointmentResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /patients/{patientId}/visit-history:
    get:
      tags: [Patients]
      summary: View a patient's visit history
      operationId: getPatientVisitHistory
      parameters:
        - $ref: '#/components/parameters/PatientId'
      responses:
        '200':
          description: Visit history summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VisitHistory'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /doctors:
    get:
      tags: [Doctors]
      summary: Search doctors
      operationId: listDoctors
      parameters:
        - name: q
          in: query
          required: false
          description: Case-insensitive search term for doctor name or specialization.
          schema:
            type: string
        - name: specialization
          in: query
          required: false
          description: Case-insensitive specialization filter.
          schema:
            type: string
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/SizeParam'
      responses:
        '200':
          description: Paginated doctor list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUserProfileResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /doctors/{doctorId}:
    get:
      tags: [Doctors]
      summary: Get doctor profile
      operationId: getDoctorById
      parameters:
        - $ref: '#/components/parameters/DoctorId'
      responses:
        '200':
          description: Doctor profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /doctors/{doctorId}/schedule:
    get:
      tags: [Doctors]
      summary: View a doctor's schedule
      operationId: getDoctorSchedule
      parameters:
        - $ref: '#/components/parameters/DoctorId'
      responses:
        '200':
          description: Doctor schedule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Schedule'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /appointments:
    post:
      tags: [Appointments]
      summary: Book an appointment
      operationId: bookAppointment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppointmentCreate'
      responses:
        '201':
          description: Appointment booked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Appointment'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'
    get:
      tags: [Appointments]
      summary: List appointments (paginated)
      operationId: listAppointments
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/SizeParam'
      responses:
        '200':
          description: Paginated appointment list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAppointmentResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /appointments/{appointmentId}:
    get:
      tags: [Appointments]
      summary: Get appointment details
      operationId: getAppointmentById
      parameters:
        - $ref: '#/components/parameters/AppointmentId'
      responses:
        '200':
          description: Appointment details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Appointment'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      tags: [Appointments]
      summary: Reschedule an appointment
      operationId: rescheduleAppointment
      parameters:
        - $ref: '#/components/parameters/AppointmentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppointmentRescheduleRequest'
      responses:
        '200':
          description: Appointment rescheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Appointment'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /appointments/{appointmentId}/cancel:
    post:
      tags: [Appointments]
      summary: Cancel an appointment
      operationId: cancelAppointment
      parameters:
        - $ref: '#/components/parameters/AppointmentId'
      responses:
        '200':
          description: Appointment cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Appointment'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /appointments/{appointmentId}/note:
    get:
      tags: [Appointments]
      summary: Get the clinical note for an appointment
      operationId: getAppointmentNote
      parameters:
        - $ref: '#/components/parameters/AppointmentId'
      responses:
        '200':
          description: Clinical note
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClinicalNote'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    put:
      tags: [Appointments]
      summary: Create or replace the clinical note for an appointment
      operationId: upsertAppointmentNote
      parameters:
        - $ref: '#/components/parameters/AppointmentId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClinicalNoteInput'
      responses:
        '200':
          description: Clinical note saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClinicalNote'
        '201':
          description: Clinical note created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClinicalNote'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      tags: [Appointments]
      summary: Delete the clinical note for an appointment
      operationId: deleteAppointmentNote
      parameters:
        - $ref: '#/components/parameters/AppointmentId'
      responses:
        '204':
          description: Clinical note deleted
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /appointments/{appointmentId}/notifications:
    get:
      tags: [Notifications, Appointments]
      summary: List notifications for an appointment (paginated)
      operationId: listAppointmentNotifications
      parameters:
        - $ref: '#/components/parameters/AppointmentId'
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/SizeParam'
      responses:
        '200':
          description: Paginated appointment notifications
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedNotificationResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /notifications:
    get:
      tags: [Notifications]
      summary: List notifications (paginated)
      operationId: listNotifications
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/SizeParam'
      responses:
        '200':
          description: Paginated notification list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedNotificationResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      tags: [Notifications]
      summary: Create a notification
      operationId: createNotification
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationCreate'
      responses:
        '201':
          description: Notification created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /notifications/{notificationId}:
    get:
      tags: [Notifications]
      summary: Get a notification
      operationId: getNotificationById
      parameters:
        - $ref: '#/components/parameters/NotificationId'
      responses:
        '200':
          description: Notification
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /ai/sessions:
    get:
      tags: [AI Assistant]
      summary: List the caller's AI conversation sessions
      operationId: listAISessions
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/SizeParam'
      responses:
        '200':
          description: Paginated list of the caller's sessions, newest first
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAISessionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
    post:
      tags: [AI Assistant]
      summary: Start a new AI conversation session
      description: >
        Creates a conversation, optionally bound to a patient and/or appointment.
        When bound, every message in the session is grounded in live data fetched
        for those ids; the binding is fixed for the lifetime of the session.
      operationId: createAISession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AISessionCreateRequest'
      responses:
        '201':
          description: Session created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AISession'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /ai/sessions/{sessionId}:
    get:
      tags: [AI Assistant]
      summary: Get a session and its full message history
      operationId: getAISession
      parameters:
        - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: Session with messages, oldest first
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AISession'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      tags: [AI Assistant]
      summary: Delete a session and its messages
      operationId: deleteAISession
      parameters:
        - $ref: '#/components/parameters/SessionId'
      responses:
        '204':
          description: Session deleted
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /ai/sessions/{sessionId}/messages:
    post:
      tags: [AI Assistant]
      summary: Send a message in a session and get the assistant's reply
      description: >
        Appends the user's message to the conversation and returns the assistant's
        answer, grounded in the session's bound patient/appointment context (re-fetched
        live) and the full prior conversation. Both messages are persisted.
        When the client sends `Accept: text/event-stream` the answer is streamed as
        Server-Sent Events (a `sources` event, then `token` events, then `done`).
      operationId: createAISessionMessage
      parameters:
        - $ref: '#/components/parameters/SessionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AIMessageRequest'
      responses:
        '200':
          description: The assistant's reply
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIMessageResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'
        '502':
          $ref: '#/components/responses/BadGateway'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    UserId:
      name: userId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    PatientId:
      name: patientId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    DoctorId:
      name: doctorId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ClinicId:
      name: clinicId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    AppointmentId:
      name: appointmentId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SessionId:
      name: sessionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    NotificationId:
      name: notificationId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    PageParam:
      name: page
      in: query
      required: false
      description: Zero-based page index.
      schema:
        type: integer
        minimum: 0
        default: 0
    SizeParam:
      name: size
      in: query
      required: false
      description: Number of items per page.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
  responses:
    BadRequest:
      description: The request is malformed or fails validation.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    Unauthorized:
      description: Authentication is required or has failed.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    Forbidden:
      description: The caller is authenticated but not allowed to access the resource.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    NotFound:
      description: The requested resource does not exist.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    Conflict:
      description: The request conflicts with the current state of the resource.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    InternalServerError:
      description: An unexpected error occurred while processing the request.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    BadGateway:
      description: An upstream service the request depends on could not be reached.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
  schemas:
    ProblemDetail:
      type: object
      description: |
        Problem Details object as defined by RFC 9457
        (https://www.rfc-editor.org/rfc/rfc9457).
      required: [title, status]
      properties:
        type:
          type: string
          format: uri
          description: A URI reference identifying the problem type.
          default: about:blank
        title:
          type: string
          description: A short, human-readable summary of the problem type.
        status:
          type: integer
          format: int32
          description: The HTTP status code generated by the origin server.
          minimum: 100
          maximum: 599
        detail:
          type: string
          description: A human-readable explanation specific to this occurrence.
        instance:
          type: string
          format: uri
          description: A URI reference that identifies the specific occurrence.
        errors:
          type: array
          description: Optional list of field-level validation errors.
          items:
            $ref: '#/components/schemas/ValidationError'
    ValidationError:
      type: object
      required: [field, message]
      properties:
        field:
          type: string
        message:
          type: string
    PageMeta:
      type: object
      required: [page, size, totalElements, totalPages]
      properties:
        page:
          type: integer
          minimum: 0
          description: Zero-based page index of the returned slice.
        size:
          type: integer
          minimum: 1
          description: Number of items per page.
        totalElements:
          type: integer
          format: int64
          minimum: 0
          description: Total number of items across all pages.
        totalPages:
          type: integer
          minimum: 0
          description: Total number of pages.
    PaginatedUserProfileResponse:
      type: object
      required: [content, page]
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/UserProfile'
        page:
          $ref: '#/components/schemas/PageMeta'
    PaginatedAppointmentResponse:
      type: object
      required: [content, page]
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
        page:
          $ref: '#/components/schemas/PageMeta'
    PaginatedNotificationResponse:
      type: object
      required: [content, page]
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/Notification'
        page:
          $ref: '#/components/schemas/PageMeta'
    UserRole:
      type: string
      enum: [PATIENT, DOCTOR, ADMIN]
    AppointmentStatus:
      type: string
      enum: [SCHEDULED, CANCELLED, RESCHEDULED, COMPLETED]
    NotificationChannel:
      type: string
      description: Delivery channel used to send a notification.
      enum: [EMAIL, SMS, PUSH]
    UserProfile:
      type: object
      required: [id, name, email, role]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
          format: email
        role:
          $ref: '#/components/schemas/UserRole'
        clinicId:
          type: string
          format: uuid
        phoneNumber:
          type: string
        dateOfBirth:
          type: string
          format: date
        specialization:
          type: string
        licenseNumber:
          type: string
        password:
          type: string
          writeOnly: true
    Clinic:
      type: object
      required: [id, name, address]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        address:
          type: string
        phoneNumber:
          type: string
    Schedule:
      type: object
      required: [doctorId, slots]
      properties:
        doctorId:
          type: string
          format: uuid
        slots:
          type: array
          items:
            $ref: '#/components/schemas/ScheduleSlot'
    ScheduleSlot:
      type: object
      required: [startAt, endAt, available]
      properties:
        startAt:
          type: string
          format: date-time
        endAt:
          type: string
          format: date-time
        available:
          type: boolean
    Appointment:
      type: object
      required: [id, patientId, doctorId, dateTime, status, duration]
      properties:
        id:
          type: string
          format: uuid
        patientId:
          type: string
          format: uuid
        doctorId:
          type: string
          format: uuid
        dateTime:
          type: string
          format: date-time
        status:
          $ref: '#/components/schemas/AppointmentStatus'
        duration:
          type: integer
          description: Duration in minutes
        reason:
          type: string
    AppointmentCreate:
      type: object
      required: [patientId, doctorId, dateTime, duration]
      properties:
        patientId:
          type: string
          format: uuid
        doctorId:
          type: string
          format: uuid
        dateTime:
          type: string
          format: date-time
        duration:
          type: integer
          minimum: 1
        reason:
          type: string
    AppointmentRescheduleRequest:
      type: object
      required: [dateTime]
      properties:
        dateTime:
          type: string
          format: date-time
        duration:
          type: integer
    ClinicalNote:
      type: object
      required: [id, appointmentId, doctorId, content, createdAt]
      properties:
        id:
          type: string
          format: uuid
        appointmentId:
          type: string
          format: uuid
        doctorId:
          type: string
          format: uuid
        content:
          type: string
        diagnosis:
          $ref: '#/components/schemas/Diagnosis'
        createdAt:
          type: string
          format: date-time
    ClinicalNoteInput:
      type: object
      properties:
        content:
          type: string
        diagnosis:
          $ref: '#/components/schemas/Diagnosis'
    Diagnosis:
      type: object
      required: [code, description]
      properties:
        code:
          type: string
        description:
          type: string
    Notification:
      type: object
      required: [id, message, sentAt, channel]
      properties:
        id:
          type: string
          format: uuid
        appointmentId:
          type: string
          format: uuid
        patientId:
          type: string
          format: uuid
        message:
          type: string
        channel:
          $ref: '#/components/schemas/NotificationChannel'
        sentAt:
          type: string
          format: date-time
    NotificationCreate:
      type: object
      required: [message, channel]
      properties:
        appointmentId:
          type: string
          format: uuid
        patientId:
          type: string
          format: uuid
        message:
          type: string
        channel:
          $ref: '#/components/schemas/NotificationChannel'
    VisitHistory:
      type: object
      required: [patientId, appointments]
      properties:
        patientId:
          type: string
          format: uuid
        appointments:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
        notes:
          type: array
          items:
            $ref: '#/components/schemas/ClinicalNote'
    AISessionCreateRequest:
      type: object
      description: >
        Options for a new conversation. Binding a patient and/or appointment
        grounds every message in the session in that record's live data.
      properties:
        patientId:
          type: string
          format: uuid
        appointmentId:
          type: string
          format: uuid
        title:
          type: string
          description: Optional human-friendly label for the conversation.
    AIMessageRole:
      type: string
      enum: [user, assistant]
    AIMessage:
      type: object
      required: [id, role, content, createdAt]
      properties:
        id:
          type: string
          format: uuid
        role:
          $ref: '#/components/schemas/AIMessageRole'
        content:
          type: string
        sources:
          type: array
          description: Grounding sources cited for an assistant message.
          items:
            type: string
        createdAt:
          type: string
          format: date-time
    AISessionSummary:
      type: object
      required: [id, userId, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        patientId:
          type: string
          format: uuid
        appointmentId:
          type: string
          format: uuid
        title:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    AISession:
      allOf:
        - $ref: '#/components/schemas/AISessionSummary'
        - type: object
          required: [messages]
          properties:
            messages:
              type: array
              items:
                $ref: '#/components/schemas/AIMessage'
    PaginatedAISessionResponse:
      type: object
      required: [content, page]
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/AISessionSummary'
        page:
          $ref: '#/components/schemas/PageMeta'
    AIMessageRequest:
      type: object
      required: [query]
      properties:
        query:
          type: string
    AIMessageResponse:
      type: object
      required: [answer]
      properties:
        answer:
          type: string
        sources:
          type: array
          items:
            type: string
        confidence:
          type: number
          format: float
    RegisterRequest:
      type: object
      required: [name, email, password, phoneNumber, dateOfBirth]
      properties:
        name:
          type: string
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          minLength: 8
        phoneNumber:
          type: string
        dateOfBirth:
          type: string
          format: date
        role:
          $ref: '#/components/schemas/UserRole'
    PasswordChangeRequest:
      type: object
      required: [currentPassword, newPassword]
      properties:
        currentPassword:
          type: string
          format: password
        newPassword:
          type: string
          format: password
          minLength: 8
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
    AuthSession:
      type: object
      required: [accessToken, user]
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string
        user:
          $ref: '#/components/schemas/UserProfile'
