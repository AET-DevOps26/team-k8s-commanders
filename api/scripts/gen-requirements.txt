# Pinned tools for gen-all.sh's Python HTTP-client generation (openapi-python-client).
# Kept in a dedicated venv (api/scripts/.venv) so the generated client is
# reproducible, the way openapitools.json pins the Java/TS generator.
# ruff is pinned too: openapi-python-client shells out to it to format and prune
# imports, so its version affects the vendored output.
openapi-python-client==0.29.0
ruff==0.15.21
