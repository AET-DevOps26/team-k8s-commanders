#!/usr/bin/env bash
set -euo pipefail

checkout_type="${3:-0}"
if [[ "$checkout_type" != "1" ]]; then
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel)"

exec "$repo_root/api/scripts/gen-all.sh"