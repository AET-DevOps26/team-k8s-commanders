# == FastAPI requirements ==
fastapi==0.139.2
uvicorn==0.51.0
python-dotenv==1.2.2
pydantic==2.13.4

# == LLM Client ==
langchain-core==1.4.9
langchain-openai==1.3.5

# == Backend service calls (live patient/notes context) ==
# httpx + attrs back the generated `caredesk_client` (see api/scripts/gen-all.sh).
httpx==0.28.1
attrs==26.1.0

# == Persistence (conversation sessions) ==
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0

# == Metrics ==
prometheus-fastapi-instrumentator==8.0.2

# == Tracing (spans exported to Tempo via OTLP/HTTP) ==
# The 1.x (core) and 0.Nb0 (instrumentation) version numbers are versioned
# independently but released in lockstep — they must be bumped together.
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
# Instruments the httpx calls the generated caredesk_client makes to
# patient-service/notes-service so their spans chain into the same trace.
opentelemetry-instrumentation-httpx==0.65b0
