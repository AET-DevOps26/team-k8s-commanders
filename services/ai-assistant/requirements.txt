# == FastAPI requirements ==
fastapi==0.136.3
uvicorn==0.49.0
python-dotenv==1.2.2
pydantic==2.13.4

# == LLM Client ==
langchain-core==1.4.6
langchain-openai==1.2.2

# == Vector store / RAG (clinical-guidelines knowledge base) ==
# PGVector store reuses the ai-db Postgres (pgvector extension); embeddings go
# through langchain-openai's OpenAIEmbeddings (also used for the local Ollama
# /v1 endpoint), so no extra embedding client is needed.
langchain-postgres==0.0.16
langchain-text-splitters==1.1.2
pgvector==0.3.6

# == Backend service calls (live patient/notes context) ==
httpx==0.28.1

# == Persistence (conversation sessions) ==
sqlalchemy[asyncio]==2.0.44
asyncpg==0.30.0
