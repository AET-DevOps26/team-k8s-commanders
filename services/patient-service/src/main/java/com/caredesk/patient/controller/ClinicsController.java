package com.caredesk.patient.controller;

import org.openapitools.api.ClinicsApi;
import org.springframework.stereotype.Controller;

// Scaffold: implements ClinicsApi so Spring registers the routes. Default
// method behaviour (501) holds until the real handlers are added later.
@Controller
public class ClinicsController implements ClinicsApi {
}
