package com.caredesk.patient.controller;

import org.openapitools.api.ClinicsApi;
import org.springframework.stereotype.Controller;

/**
 * Scaffold controller for the {@code /clinics/**} endpoints.
 *
 * <p>Implements {@link ClinicsApi} so Spring registers the routes. The
 * default 501 behaviour holds until the real handlers are added in a follow
 * up issue.
 */
@Controller
public class ClinicsController implements ClinicsApi {
}
