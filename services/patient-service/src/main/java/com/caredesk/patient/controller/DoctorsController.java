package com.caredesk.patient.controller;

import org.openapitools.api.DoctorsApi;
import org.springframework.stereotype.Controller;

// Scaffold: implements DoctorsApi so Spring registers the routes. Default
// method behaviour (501) holds until the real handlers land in #35.
@Controller
public class DoctorsController implements DoctorsApi {
}
